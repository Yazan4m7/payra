<?php
use Illuminate\Database\Migrations\Migration; use Illuminate\Database\Schema\Blueprint; use Illuminate\Support\Facades\Schema;
return new class extends Migration { public function up(): void {
 Schema::create('leave_balances', function(Blueprint $t){$t->id();$t->foreignId('employee_id')->constrained()->cascadeOnDelete();$t->unsignedSmallInteger('year');$t->foreignId('compliance_setting_id')->constrained('compliance_settings')->restrictOnDelete();foreach(['annual_entitlement','annual_used','annual_remaining','sick_entitlement','sick_used','sick_remaining','hospital_extra_entitlement','hospital_extra_used','hospital_extra_remaining'] as $c)$t->decimal($c,8,2)->default(0);$t->timestamps();$t->softDeletes();$t->unique(['employee_id','year']);});
 Schema::create('leave_requests', function(Blueprint $t){$t->id();$t->foreignId('employee_id')->constrained()->cascadeOnDelete();$t->enum('type',['annual','sick'])->index();$t->date('start_date');$t->date('end_date');$t->decimal('days',8,2);$t->boolean('hospitalized')->default(false);$t->enum('status',['pending','approved','rejected','cancelled'])->default('pending')->index();$t->foreignId('approver_id')->nullable()->constrained('users')->nullOnDelete();$t->timestamp('approved_at')->nullable();$t->text('reason')->nullable();$t->timestamps();$t->softDeletes();});
 Schema::create('overtime_entries', function(Blueprint $t){$t->id();$t->foreignId('employee_id')->constrained()->cascadeOnDelete();$t->date('date')->index();$t->decimal('hours',8,2);$t->enum('rate_type',['standard','rest_holiday']);$t->enum('status',['pending','approved','rejected'])->default('pending')->index();$t->foreignId('approver_id')->nullable()->constrained('users')->nullOnDelete();$t->timestamp('approved_at')->nullable();$t->text('notes')->nullable();$t->timestamps();$t->softDeletes();$t->index(['employee_id','date','status']);});
 } public function down(): void {Schema::dropIfExists('overtime_entries');Schema::dropIfExists('leave_requests');Schema::dropIfExists('leave_balances');} };
