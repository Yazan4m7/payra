<?php
use Illuminate\Database\Migrations\Migration; use Illuminate\Database\Schema\Blueprint; use Illuminate\Support\Facades\Schema;
return new class extends Migration { public function up():void{Schema::create('employee_documents',function(Blueprint $t){$t->id();$t->foreignId('employee_id')->constrained()->cascadeOnDelete();$t->string('category',80)->index();$t->string('original_name');$t->string('storage_path',500)->unique();$t->string('mime_type',150);$t->unsignedBigInteger('size_bytes');$t->char('sha256',64)->index();$t->date('expires_at')->nullable()->index();$t->foreignId('uploaded_by')->nullable()->constrained('users')->nullOnDelete();$t->text('notes')->nullable();$t->timestamps();$t->softDeletes();});} public function down():void{Schema::dropIfExists('employee_documents');}};
