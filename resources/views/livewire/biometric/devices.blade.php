<div>@if($newToken)<div class="alert alert-warning"><strong>Copy this device token now; it will not be shown again:</strong><div class="font-monospace text-break">{{$newToken}}</div></div>@endif<div class="row g-3"><div class="col-lg-5"><div class="card"><div class="card-header">Biometric devices</div><div class="card-body"><form wire:submit="create" class="vstack gap-2"><input class="form-control" placeholder="Device name" wire:model="name"><input class="form-control" placeholder="Timezone" wire:model="timezone"><button class="btn btn-primary">Create device</button></form></div><ul class="list-group list-group-flush">@foreach($devices as $d)<li class="list-group-item">{{$d->name}} · {{$d->uuid}} <small>{{$d->last_seen_at?:'never seen'}}</small></li>@endforeach</ul></div></div><div class="col-lg-7"><div class="card"><div class="card-header">Employee mappings</div><div class="card-body"><form wire:submit="map" class="row g-2"><div class="col-4"><select class="form-select" wire:model="deviceId"><option value="">Device</option>@foreach($devices as $d)<option value="{{$d->id}}">{{$d->name}}</option>@endforeach</select></div><div class="col-4"><select class="form-select" wire:model="employeeId"><option value="">Employee</option>@foreach($employees as $e)<option value="{{$e->id}}">{{$e->name}}</option>@endforeach</select></div><div class="col-3"><input class="form-control" placeholder="Device employee ID" wire:model="externalEmployeeId"></div><div class="col-1"><button class="btn btn-primary">Map</button></div></form></div><ul class="list-group list-group-flush">@foreach($mappings as $m)<li class="list-group-item">{{$m->device->name}}: {{$m->external_employee_id}} → {{$m->employee->name}}</li>@endforeach</ul></div></div></div></div>
